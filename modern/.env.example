# SQL Express connection (host, not containerized)
# Used by API container to reach host SQL Express via host.docker.internal
SQL_PASSWORD=YourPassword123!

# Example connection string template:
# Server=host.docker.internal,1433;Database=LegacyEcommerceDb;User Id=sa;Password=${SQL_PASSWORD};TrustServerCertificate=True
