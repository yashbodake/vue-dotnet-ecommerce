import { defineStore } from 'pinia'

export const useAppStore = defineStore('app', {
  state: () => ({
    // Placeholder state - will be extended in later slices
  }),
  actions: {
    // Placeholder actions - will be extended in later slices
  }
})
